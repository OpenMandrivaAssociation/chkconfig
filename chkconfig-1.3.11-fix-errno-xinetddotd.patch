--- chkconfig.c	2008-12-25 00:03:58.000000000 +0100
+++ chkconfig.c.oden	2008-12-25 00:04:17.000000000 +0100
@@ -458,7 +458,7 @@ static int listService(char * item) {
 	    printf(_("xinetd based services:\n"));
 	    if (!(dir = opendir(XINETDDIR))) {
 		    fprintf(stderr, _("failed to open directory %s: %s\n"),
-			    XINETDDIR, strerror(err));
+			    XINETDDIR, strerror(errno));
 		    return 1;
 	    }
 	    numServices = 0;
