diff --git a/.tx/config b/.tx/config
new file mode 100644
index 0000000..344a7c1
--- /dev/null
+++ b/.tx/config
@@ -0,0 +1,8 @@
+[main]
+host = https://www.transifex.net
+
+[chkconfig.default]
+file_filter = po/<lang>.po
+source_file = po/chkconfig.pot
+source_lang = en
+
diff --git a/chkconfig.8 b/chkconfig.8
index afe92f3..24a0d34 100644
--- a/chkconfig.8
+++ b/chkconfig.8
@@ -4,7 +4,7 @@
 chkconfig \- updates and queries runlevel information for system services
 
 .SH SYNOPSIS
-\fBchkconfig\fR [\fB--list\fR] [\fB--type\fR \fItype\fR][\fIname\fR]
+\fBchkconfig\fR [\fB--list\fR] [\fB--type\fR \fItype\fR] [\fIname\fR]
 .br
 \fBchkconfig\fR \fB--add\fR \fIname\fR
 .br
@@ -12,9 +12,9 @@ chkconfig \- updates and queries runlevel information for system services
 .br
 \fBchkconfig\fR \fB--override\fR \fIname\fR
 .br
-\fBchkconfig\fR [\fB--level\fR \fIlevels\fR] [\fB--type\fR \fItype\fR] \fIname\fR <\fBon\fR|\fBoff\fR|\fBreset\fR|\fBresetpriorities\fR>
+\fBchkconfig\fR [\fB--level\fR \fIlevels\fR] [\fB--type\fR \fItype\fR] [\fB--no-redirect\fR] \fIname\fR <\fBon\fR|\fBoff\fR|\fBreset\fR|\fBresetpriorities\fR>
 .br
-\fBchkconfig\fR [\fB--level\fR \fIlevels\fR] [\fB--type\fR \fItype\fR] \fIname\fR 
+\fBchkconfig\fR [\fB--level\fR \fIlevels\fR] [\fB--type\fR \fItype\fR] [\fB--no-redirect\fR] \fIname\fR 
 .br
 
 .SH DESCRIPTION
@@ -81,6 +81,15 @@ a string of numbers from 0 to 6. For example, \fB--level 35\fR specifies
 runlevels 3 and 5.
 
 .TP
+\fB-\-no-redirect\fR
+When chkconfig is run on a system that uses systemd as its init system,
+chkconfig will forward commands to systemd if a systemd service
+file exists for it.  This switch turns off the redirection to systemd and
+only operates on the symlinks in /etc/rc[0-6].d. This option is only
+valid when \fBon\fR, \fBoff\fR, or no command (to check enablement)
+is passed to a service.
+
+.TP
 \fB-\-add\fR \fIname\fR
 
 This option adds a new service for management by \fBchkconfig\fR.
diff --git a/chkconfig.c b/chkconfig.c
index 686d789..d337e86 100644
--- a/chkconfig.c
+++ b/chkconfig.c
@@ -24,6 +24,8 @@
 #include <stdlib.h>
 #include <string.h>
 #include <sys/stat.h>
+#include <sys/types.h>
+#include <sys/wait.h>
 #include <unistd.h>
 
 static char *progname;
@@ -38,6 +40,10 @@ static int LSB = 0;
 #define SYSTEMD_SERVICE_PATH "/lib/systemd/system"
 #endif
 
+#ifndef SYSTEMD_BINARY_PATH
+#define SYSTEMD_BINARY_PATH "/bin/systemd"
+#endif
+
 static void usage(void) {
     fprintf(stderr, _("%s version %s - Copyright (C) 1997-2000 Red Hat, Inc.\n"), progname, VERSION);
     fprintf(stderr, _("This may be freely redistributed under the terms of "
@@ -73,10 +79,16 @@ static void checkRoot() {
 }
 
 static void reloadSystemd(void) {
+    struct stat a, b;
 
-    if (access(SYSTEMD_SERVICE_PATH, F_OK) >= 0) {
-        system("systemctl daemon-reload > /dev/null 2>&1");
-    }
+    if (lstat("/sys/fs/cgroup", &a) < 0)
+	return;
+    if (lstat("/sys/fs/cgroup/systemd", &b) < 0)
+	return;
+    if (a.st_dev == b.st_dev)
+	return;
+
+    system("systemctl daemon-reload");
 }
 
 static int delService(char *name, int type, int level) {
@@ -594,7 +606,7 @@ void forwardSystemd(const char *name, int type, const char *verb) {
 }
 
 int main(int argc, const char ** argv) {
-    int listItem = 0, addItem = 0, delItem = 0, overrideItem = 0;
+    int listItem = 0, addItem = 0, delItem = 0, overrideItem = 0, noRedirectItem = 0;
     int type = TYPE_ANY;
     int rc, i, x;
     char * levels = NULL;
@@ -606,6 +618,7 @@ int main(int argc, const char ** argv) {
 	    { "add", '\0', 0, &addItem, 0 },
 	    { "del", '\0', 0, &delItem, 0 },
 	    { "override", '\0', 0, &overrideItem, 0 },
+	    { "no-redirect", '\0', 0, &noRedirectItem, 0},
 	    { "list", '\0', 0, &listItem, 0 },
 	    { "level", '\0', POPT_ARG_STRING, &levels, 0 },
 	    { "levels", '\0', POPT_ARG_STRING, &levels, 0 },
@@ -668,6 +681,10 @@ int main(int argc, const char ** argv) {
 	exit(1);
     }
 
+    if (getenv("SYSTEMCTL_SKIP_REDIRECT") != NULL) {
+        noRedirectItem = 1;
+    }
+
     if (addItem) {
 	char * name = (char *)poptGetArg(optCon);
         int r;
@@ -707,7 +724,8 @@ int main(int argc, const char ** argv) {
 
 	if (item && poptGetArg(optCon)) usage();
 
-        if (access(SYSTEMD_SERVICE_PATH, F_OK) >= 0) {
+        if (access(SYSTEMD_SERVICE_PATH, F_OK) >= 0 &&
+            access(SYSTEMD_BINARY_PATH, F_OK) >= 0) {
 	    fprintf(stderr, _("\nNote: This output shows SysV services only and does not include native\n"
                               "      systemd services. SysV configuration data might be overriden by native\n"
                               "      systemd configuration.\n\n"));
@@ -728,8 +746,9 @@ int main(int argc, const char ** argv) {
 	}
 
 	if (!state) {
-
-            forwardSystemd(name, type, "is-enabled");
+	    if (!noRedirectItem) {
+		forwardSystemd(name, type, "is-enabled");
+	    }
 
 	    if (where) {
 		rc = x = 0;
@@ -760,10 +779,14 @@ int main(int argc, const char ** argv) {
 	    } else
 	       return isOn(name, level) ? 0 : 1;
 	} else if (!strcmp(state, "on")) {
-            forwardSystemd(name, type, "enable");
-            return setService(name, type, where, 1);
+	    if (!noRedirectItem) {
+		forwardSystemd(name, type, "enable");
+	    }
+	    return setService(name, type, where, 1);
         } else if (!strcmp(state, "off")) {
-            forwardSystemd(name, type, "disable");
+	    if (!noRedirectItem) {
+		forwardSystemd(name, type, "disable");
+	    }
 	    return setService(name, type, where, 0);
         } else if (!strcmp(state, "reset"))
 	    return setService(name, type, where, -1);
diff --git a/po/bal.po b/po/bal.po
index 5293afe..2503001 100644
--- a/po/bal.po
+++ b/po/bal.po
@@ -389,7 +389,7 @@ msgstr "%s حالت اتونت.\n"
 #: ../alternatives.c:662
 #, c-format
 msgid "%s - status is manual.\n"
-msgstr "%a حالت دستینت.\n"
+msgstr "%s حالت دستینت.\n"
 
 #: ../alternatives.c:664
 #, c-format
diff --git a/po/chkconfig.pot b/po/chkconfig.pot
index cf7e9ef..9538e49 100644
--- a/po/chkconfig.pot
+++ b/po/chkconfig.pot
@@ -8,7 +8,7 @@ msgid ""
 msgstr ""
 "Project-Id-Version: PACKAGE VERSION\n"
 "Report-Msgid-Bugs-To: \n"
-"POT-Creation-Date: 2011-02-14 17:06-0500\n"
+"POT-Creation-Date: 2011-03-04 15:08-0500\n"
 "PO-Revision-Date: YEAR-MO-DA HO:MI+ZONE\n"
 "Last-Translator: FULL NAME <EMAIL@ADDRESS>\n"
 "Language-Team: LANGUAGE <LL@li.org>\n"
@@ -17,108 +17,108 @@ msgstr ""
 "Content-Type: text/plain; charset=CHARSET\n"
 "Content-Transfer-Encoding: 8bit\n"
 
-#: ../chkconfig.c:38
+#: ../chkconfig.c:46
 #, c-format
 msgid "%s version %s - Copyright (C) 1997-2000 Red Hat, Inc.\n"
 msgstr ""
 
-#: ../chkconfig.c:39
+#: ../chkconfig.c:47
 #, c-format
 msgid ""
 "This may be freely redistributed under the terms of the GNU Public License.\n"
 msgstr ""
 
-#: ../chkconfig.c:42
+#: ../chkconfig.c:50
 #, c-format
 msgid "usage:   %s [--list] [--type <type>] [name]\n"
 msgstr ""
 
-#: ../chkconfig.c:43
+#: ../chkconfig.c:51
 #, c-format
 msgid "         %s --add <name>\n"
 msgstr ""
 
-#: ../chkconfig.c:44
+#: ../chkconfig.c:52
 #, c-format
 msgid "         %s --del <name>\n"
 msgstr ""
 
-#: ../chkconfig.c:45
+#: ../chkconfig.c:53
 #, c-format
 msgid "         %s --override <name>\n"
 msgstr ""
 
-#: ../chkconfig.c:46
+#: ../chkconfig.c:54
 #, c-format
 msgid "         %s [--level <levels>] [--type <type>] <name> %s\n"
 msgstr ""
 
-#: ../chkconfig.c:53
+#: ../chkconfig.c:61
 #, c-format
 msgid ""
 "service %s supports chkconfig, but is not referenced in any runlevel (run "
 "'chkconfig --add %s')\n"
 msgstr ""
 
-#: ../chkconfig.c:55
+#: ../chkconfig.c:63
 #, c-format
 msgid "service %s does not support chkconfig\n"
 msgstr ""
 
-#: ../chkconfig.c:57
+#: ../chkconfig.c:65
 #, c-format
 msgid "error reading information on service %s: %s\n"
 msgstr ""
 
-#: ../chkconfig.c:66
+#: ../chkconfig.c:74
 #, c-format
 msgid "You do not have enough privileges to perform this operation.\n"
 msgstr ""
 
-#: ../chkconfig.c:384 ../chkconfig.c:389 ../chkconfig.c:503
+#: ../chkconfig.c:399 ../chkconfig.c:404 ../chkconfig.c:518
 msgid "on"
 msgstr ""
 
-#: ../chkconfig.c:384 ../chkconfig.c:389 ../chkconfig.c:503
+#: ../chkconfig.c:399 ../chkconfig.c:404 ../chkconfig.c:518
 msgid "off"
 msgstr ""
 
-#: ../chkconfig.c:465
+#: ../chkconfig.c:480
 #, c-format
 msgid "xinetd based services:\n"
 msgstr ""
 
-#: ../chkconfig.c:467
+#: ../chkconfig.c:482
 #, c-format
 msgid "failed to open directory %s: %s\n"
 msgstr ""
 
-#: ../chkconfig.c:575
+#: ../chkconfig.c:589
 #, c-format
 msgid "Note: Forwarding request to 'systemctl %s %s'.\n"
 msgstr ""
 
-#: ../chkconfig.c:579
+#: ../chkconfig.c:593
 #, c-format
 msgid "Failed to forward service request to systemctl: %m\n"
 msgstr ""
 
-#: ../chkconfig.c:636
+#: ../chkconfig.c:651
 #, c-format
 msgid "%s version %s\n"
 msgstr ""
 
-#: ../chkconfig.c:648
+#: ../chkconfig.c:663
 #, c-format
 msgid "--type must be 'sysv' or 'xinetd'\n"
 msgstr ""
 
-#: ../chkconfig.c:656
+#: ../chkconfig.c:671
 #, c-format
 msgid "only one of --list, --add, --del, or --override may be specified\n"
 msgstr ""
 
-#: ../chkconfig.c:689
+#: ../chkconfig.c:721
 #, c-format
 msgid ""
 "\n"
@@ -129,7 +129,7 @@ msgid ""
 "\n"
 msgstr ""
 
-#: ../chkconfig.c:725
+#: ../chkconfig.c:758
 #, c-format
 msgid "only one runlevel may be specified for a chkconfig query\n"
 msgstr ""
